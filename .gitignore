.venv/
.purtera_artifacts/
tmp_quote_test.xlsx
__pycache__/
.pytest_cache/
*.pyc
*.sqlite3
*.db
dist/
build/
*.egg-info/
real_data_cases/*/artifacts/*
real_data_cases/*/outputs/*
!real_data_cases/.gitkeep
!real_data_cases/*/labels/*.json
!real_data_cases/*/case_manifest.json
# Redundant with _handoff_* tree; use folder for source of truth
COPPER_*_parser_gap_handoff.zip

# Local overlay / extraction dumps (regenerate from PDF + detect)
compiled_artifacts/
# Parser-emitted side files (structured.json / structured.md) live next
# to the source artifact under "<stem>.derived/" — regenerated on every
# compile, so don't track them.
*.derived/
.orbitbrief/

# Claude Code worktree configuration (per-checkout, not source).
.claude/

# Local secrets — never commit (DeepSeek/teacher API keys etc.)
.env
.env.*

# Trained model weights + downloaded schematic corpus (artifacts, not code)
*.pt
_schematic_corpus/
_vision_rip_out/

# RunPod detector dataset (artifacts, not code)
runpod_detector/dataset/
runpod_detector/objectness_box_labels.jsonl
